# Week13 VLA Fine-tuning

本週以小型 NumPy Policy 說明 pretrained model + robot dataset → fine-tuning → task-specific policy。重點是 training step、validation、overfitting 與 checkpoint，不從零訓練大型 VLA。舊占位內容在 `legacy_v1/`。
